perf: optimistic updates sur cards + icone cliquable en mode Tout

Infos.tsx : les 3 mutations sur cards deviennent optimistes
- save : card apparait immediatement, sheet ferme instantanement
- updateCard : modifs visibles tout de suite
- removeCard : card disparait instantanement apres confirm
- Rollback automatique sur chaque mutation si Supabase echoue :
  * save retire la card + rouvre le sheet avec les valeurs
  * updateCard restaure la card d'origine
  * removeCard re-insere la card a sa position

InfoCardView.tsx : en mode "Tout", cliquer sur l'icone de categorie
navigue vers cette categorie (meme comportement que cliquer sur le titre).

Nettoyage : retire msg.txt du tracking Git.
